-- Script that lists all records of the second_table of the database 
-- The output should containt score and the name 
-- Records ordered by score(descending)

SELECT score, name
FROM second_table
ORDER BY score DESC
